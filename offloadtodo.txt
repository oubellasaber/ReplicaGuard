we should be tracking state changes
(download, upload) => with progress

up and dl endpoints should be ablt to report progress to some kind of services either by posting events and blocking function call

There should be a handler for those events, it does stream chnages to the client and also save to db the updates to later be analyzed


domain handle statusUpdated event firing

events go to queue =====>  goes into consumer that pushed updates into clints


// Make replica switch state methods fire events 
ReplicaStatusMadeProgress(
    Guid AssetId, 
    Guid ReplicaId, 
    ReplicaStatus ReplicaStatus,
    long? BytesProgress,
    DateTime DateTimeUtc);


each fire goes into progress-queue then handled later by a consumer that consumes on the right order (DateTimeUtc ASC)

progress streamer streams progress to clients.

what im woried about is when for example uploader reports progress hed do it this way update domian model (Replica)  replica updates its state and the prgress table and fire event
and after this we ahve to savechnages (this is my concern)
